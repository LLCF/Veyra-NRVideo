# XeSS 撕裂、卡顿与 GC573 RGB 掉速：三份日志联合排查

日期：2026-09-19。状态：诊断与施工方案，尚未实施本方案新增修复。

当前分支 `codex/capture-ui-sync-20260919` 有上一轮未提交修复，保留原状。本轮不构建、不打包、不发布。已有修复的验收范围见 `CAPTURE_UI_SYNC_ACCEPTANCE_2026-09-19.md`，不能当作这三位用户通过的证据。

## 1. 输入与证据边界

原始日志均在 `C:/Users/123/Desktop/`；下文时间保留日志原始 Z 时间，不换算。文件分别为：

|编号|文件|主要来源|SHA256|
|---|---|---|---|
|A|画面撕裂，xess卡顿的，功率不稳定.log|RTX 5080，ASUS 4K PRO，P010/NV12 多次切换|EF8CC4897FF7D75751DF374D856509871412D19B1C6E9034A4B3A512BDDD6CF8|
|B|573 采集RGB60fps模式卡53fps.log|RTX 5090，GC573，多种采集模式|82AD479467D20CEDDEA77B61D39DCF963142631B0764CC85D4ED2B188200C8A4|
|C|画面撕裂 xess卡.log|RTX 5070 Ti，PS5 RemotePlay，1080p60 H.265|9DB419ACD30FD24FE86DBF2519A7F2D4384CAA9F3119AC369C64F0A0620128C8|

按会话、格式、设置 revision 和提供方分别分析。C 不是采集卡；其通用 capture-timing 日志 callbackFps=0 不代表无输入。没有功率采样或物理屏幕扫描证据，不能从这些日志测出瓦数波动原因或眼睛看到的延迟。

## 2. 已证实与尚待证明

### 2.1 GC573 确有约 53.49fps 的输入吞吐，不能仅修 FPS 显示

B 第 18034–18050 行：08:24:26，协商 3840x2160、60fps、RGB24，stride=11520，样本 24883200 字节；请求 3 个缓冲，实际返回 4 个。实际缓冲数量不等于已测得的排队延迟。

效果关闭后的稳定区间：

|时间|received|processed|dropped|处理 CPU P95|GPU ready P95|Present CPU P95|
|---|---:|---:|---:|---:|---:|---:|
|08:28:00.825，第19534行|11337|10348|7|2.092ms|0.769ms|0.390ms|
|08:29:00.385，第19921行|14523|13534|7|2.014ms|0.683ms|0.289ms|

59.560 秒内 received 和 processed 都增加 3186，即约 53.49fps，丢弃数未增长。NR/Flow/SR/FG 阶段耗时为零，残留的 NR 累计计数不能解释成仍在运行。问题在完成采集回调之前或回调自身，不是这一段增强渲染算不过来。

不能据此断言硬件只输出 53fps：`CaptureCardSource.cpp` 的 received 在复制后增长；`NativeCaptureSink::Receive` 持锁调用回调，回调再持采集锁执行 `copyCaptureSample`。`CaptureMediaType.h` 的 RGB24 路径逐像素扩展 BGR24 到 BGR0，发生在驱动送帧线程。软件回调反过来阻塞驱动是明确风险，但旧日志没有复制耗时，尚不能定为唯一根因。

### 2.2 XeSS 防撕裂存在独立的接入问题，当前工作区已有修复

C 第 166、944 行分别记录 requested=true/2/2、true/0/1，而 effective=false；旧实现 `VideoPresenter::configurePresentation` 和 `PresentSink::configurePacing` 在 XeSS 下直接清掉 vsync。选择了显示同步却被软件关闭，属于代码确认的缺陷。

当前未提交改动已经将 XeSS 的显示同步交给提供方，同时不启用应用层队列等待。上一轮验证的是外层 Present 参数 sync=1/flags=0，不是最终生成子帧扫描，也不证明用户屏幕已无撕裂。新实现 effective.enabled=false 仅表示应用节奏控制关闭，不能再把它单独解释为 VSync 关闭。必须补充提供方最终呈现的证据。

VSync 解决扫描时换帧导致的撕裂，不能自动解决输入不足、帧间隔不均或历史频繁失效。

### 2.3 PS5 日志把 XeSS 卡顿指向解码之后的链路

C 06:35:14–24 的 remoteplay-progress：接收/解码约 58–60fps，多数为 60，received=decoded、queue=0、errors=0。该窗口 transport/assembly errors=0，packetWindowLost=0；这些是窗口指标，不能推断整个文件从未丢包。

同段 capture-timing：06:35:15.565 至 06:35:24.660，received 从 214 到 756，processed 从 126 到 630，dropped 从 86 到 124，historyResets 从 11 到 49。对应输入约 59.59fps、处理约 55.42fps，新增 38 次丢弃和 38 次重置。Present CPU P95 多次约 17–18ms，GPU ready P95 约 20–21ms；这些分位数不能相加当作单帧总延迟。

代码 `EngineController.cpp` 对 Drop/预览跳帧触发 temporalBreak；XeSS 按 motion 前帧身份不连续等条件 reset。因此“后端等待拖慢消费 -> mailbox 覆盖 -> 历史重置 -> 补帧不连续”是优先验证的机制。日志数量相关与代码路径支持此方向，但还缺逐帧等待归属和 reset 原因，不能宣称完整因果已证明。不能通过取消必要 reset 掩盖掉帧，否则会把不连续运动信息用于插帧。

C 的 inBurst 间隔曾出现 0.385–7.476ms、0.608–11.406ms，60fps 4X 的名义间隔是 4.167ms。现有 inBurst 统计排除了批次边界，既不能代表完整输出均匀，也不是扫描间隔。

### 2.4 5080 功耗波动：调度长帧与生成量变化是主线，主动限帧仅是统计边界

用户追加说明：5080 的主要症状是功耗忽高忽低。此前强调最后一段主动限帧不足以解释这个症状。进一步检查发现，无主动限帧的原生 4K30 段也有持续调度长帧；不能将其归为正常 60->30 行为。

**原生 30fps、设置不变的证据：**

- 11:22:20.299（第833行），source=1846：增强图 GPU span=14.059ms，readyObservedAge=21.215ms，deadlineWait=12.168ms，present=60.573ms，callbackToReturn=93.955ms。11:22:21.994（第848行）另一次 present=55.485ms，图 GPU span=13.027ms。这里是单帧字段，不是把不同 P95 相加。GPU span 不包含完整 XeSS 内部工作，Present 也不是纯睡眠。
- 11:27:25–34，revision=11，反复出现 engine-stall 总计约 68–69ms，其中 scheduleAndPresent 约 62–64ms，graphSubmit CPU 约 3.4–4.0ms。第3541行确认 capture60To30=false、rateSkipped=0。不是用户切换设置期间一次初始化，也不是主动跳一半帧。
- 11:27:32.198 -> 11:27:33.228，输入约 30.1fps、处理约 27.2fps，生成帧增量约 61.2fps，新增 3 次 dropped 和 3 次 historyReset；NR P95 约 14.50ms。11:27:33.158 同期 SDK result=3、framesPresented=1。输入仍在，增强/生成/输出工作量不再均匀。
- 11:22 的 NR P95 多数约 13–14ms，Present P95 却从约 5–6ms 到 30ms 以上变化。P95 只支持窗口差异，不能反推每一帧或具体瓦数。

**即使在主动限帧段，也有额外异常：**

12:58:11.738 -> 12:58:12.743，received +60、processed +27、generated +54、dropped +6、historyResets +6；此前约一秒 generated +93。NR P95 两点都是 14.346ms，Present P95 从 1.966ms 到 24.590ms，设置 revision=5 不变。随后恢复到每秒约 90 个生成帧。该段主动限帧目标稳定在 30，不能解释生成量短暂跌到约 54；这些计数存在异步完成边界，短窗口比例不是逐对生成成功率。

**多方面核对结果：**

|方向|日志/代码事实|当前判断|
|---|---|---|
|输入突然断流|原生30段增量输入仍约30；capture-discontinuity 记录集中在每次连接的最初几帧|不能用开流约100ms缺口解释几分钟后的反复卡顿；短时到达抖动仍需逐帧验证|
|NR 每帧工作量大幅变动|所选异常窗口 NR P95 相对稳定，生成量与等待变化更明显|不支持把 NR 算力波动定为主因；没有完整单帧 GPU 序列，不能完全排除|
|补帧主动预算抑制|采集 XeSS 不走图内 FgRecoveryBudget admission；日志无 xess-fg-gate 事件|不能套用 DLSS 预算反复拒绝的旧诊断；enable/reset 的变化仍须检查|
|呈现拖住下一次增强|单 GPU owner 主循环同步调用 presenter.present；队列容量2，推进过程中 Present 返回前不能继续该线程的源处理|已确认代码耦合，且长帧主要在调度/呈现；是最高优先级机制|
|多层等待相互影响|当前路径有 XeLL beginInput sleep、采集时间线 deadline、提供方子帧 scheduler|各自有不同职责，不能仅数等待点就认定重复；需量出各自期限、用时及 GPU 空洞|
|提供方子帧节奏|11:22:02.424 inBurst min=0.615ms/max=8.879ms；11:22:06.438 min=0.664ms/max=8.344ms，名义目标8.333ms；refused/bypassed=0|有短间隔，且该段不是 fallback 所致；只覆盖批内，缺批间完整证据|
|场景/历史变化|掉帧、historyReset、AMD OF reset 与生成下降同段出现|需区分 Drop、scene cut、参数变更、ID不连续，不能一律取消reset|
|CPU日志/UI/重建|所选稳定长帧 periodicLogs/tail 很小；初始化及切换另有秒级stall|不能把切换重建计入稳态；没有证据表明日志写入是所选长帧主因|
|显存/驱动功率管理|初始化预算约15GiB，记录的图下限约1GiB，但不含全部SDK分配；无瓦数/频率/节流遥测|没有足够证据认定显存耗尽或降频；也不能据初始化预算排除运行时驻留问题|

最符合当前证据的解释是：调度/呈现长帧打断主循环供给，部分源帧被覆盖并触发历史重置，生成工作量随之下降，再恢复。它可以导致用户观察到的负载/功耗起伏，但功耗因果仍须同步遥测确认；不能把 Present 60ms 直接称为 GPU 空闲60ms。

A 最后一段输入约 59.95fps，但明确启用了 60 -> 30 的采集限帧。结束时 sourceAccepted=3190、sourceSkippedBeforeGraph=3189，约一半跳过与用户设置一致。需另查 mailbox 丢弃和重置，不能把 3189 全计入故障。

A 11:22:02.918 有 enabled=0/framesPresented=1，11:22:06.942 有 enabled=1/result=3/framesPresented=1，说明存在没有达到 4X 的时段。本地 XeSS 3.0.2 头文件将 3 定义为 WARNING_TOO_FEW_FRAMES，注释为 Frame ID should be over 0；不得误译为显卡性能不足。需要将它与 enable/reset/Frame ID 串联定位。

原生 P010/NV12/RGB 路径与此前审查发现的压缩采集包丢弃、EAGAIN、重排 PTS 问题不是同一条路径，不把后者强行当成这三份日志的根因。

## 2.5 DLSS 4X/6X 补帧受限追加审查

用户追加反馈多用户4X/6X受限。不能假设补帧成本恒小：当前 `src/pipeline/EnhanceGraph.cpp:1902` 对4X执行3次、6X执行5次 Evaluate，另有资源转换、状态读取和提交；实际成本须按分辨率、设备、倍率测量。GPU低利用率也可能是输入不足、显示反压或软件拒绝工作。

### 已确认的代码机制

1. `EngineController.cpp:1186` 的计算前拒绝更新 `lastFgRejected100ns`，界面在约1秒内标为受限。该标志不是GPU饱和判据。XeSS走呈现端，不使用此图内预算，必须分别诊断。
2. `EngineController.cpp:1289` 的 Present 服务样本只扣除 allocator 等待，仍包含 DXGI/驱动阻塞；`FgRecoveryBudget::admit` 再将其P95加到预计剩余工作。显示反压可以导致尚未计算的整组FG被拒绝。当前单测甚至明确覆盖“80ms Present导致拒绝”，但只验证样本过期恢复，未覆盖周期性显示阻塞与持续生成。
3. `EnhanceGraph.cpp:1892` 一次决定整组是否执行；拒绝置 `fgHistorySkipped_`。下一次获准会 reset 并执行全部子帧，但 `hasGenerated=false`，再下一对连续获准才可能输出。一次拒绝至少损失本组和随后暖机组的生成机会；交替拒绝/获准可以持续算暖机却没有有效补帧。不能直接删掉必要reset，否则历史错误会变为画面故障。
4. 预算使用最后一个子帧的期限，呈现按每个子帧期限另做过期淘汰（采集容差10ms）。所以“获准计算”不保证3/5个子帧全部赶得上。`LivePairLatency` 当前只允许比旧相位更早，不能超出“一源帧间隔+base成本上限”；需要检查其对较慢FG和6X首子帧是否足够，不能未经测量直接全局增加延迟。
5. 预算的 graph span 是同队列首尾GPU时间戳包络，包含依赖等待，并非各阶段纯执行耗时之和。CPU elapsed 也不等于GPU已完成量：admission前有条件提交点，但最后一段base在admission后才提交。必须拆开执行、依赖、供给空洞和显示等待，不能重复计费或把CPU忙碌当GPU进度。

算式示例（仅模型，不是实机测试）：预算预测12ms、base进度抵扣2ms、剩余期限8ms，容差10ms时，无Present等待需要10ms，可获准；Present P95为17ms时需求变成27ms，被拒绝。它证明等待可触发受限，不证明17ms阻塞必然可以安全忽略；显示吞吐确实不足时仍需有界背压。

### 日志边界与反证

A的11:35:30.863至11:35:34.929，`fgSkippedBeforeEval`始终为9，是累计值，不能把每行9都算成持续新拒绝。11:35:31/32有约121/120fps提交；11:35:33跌至94，epoch100->102、暖机15->21、生成过期3->7，而计算前拒绝未增加。这次下降更符合历史重置/过期，不能全归预算门禁。A的87条节流admission日志均为true，但累计计数证明存在漏采的拒绝事件，现有每秒采样不能还原其当时成本。

另外检查桌面旧5060日志，未找到非零 `fgSkippedBeforeEval`，不能用它证明本次4X/6X预算根因；旧5090指定日志当前不存在。本轮没有覆盖所有反馈用户的6X失败现场，也没有新增硬件实测。

### 修复顺序与验收补充

1. 优先记录拒绝事件的原因和分项成本：预算、首/末子帧期限、显示等待、GPU阶段/依赖、来源间隔、倍率、暖机状态。按窗口聚合并保留首个状态转换，避免逐帧大量日志造成干扰。UI区分计算前拒绝、过期、暖机、SDK无效、显示吞吐上限。
2. 将必需计算成本和显示反压分开建模；保留有界显示队列。不能仅把Present项归零，也不能通过关闭预算一律猛算来宣称修复。基于首子帧就绪与后续子帧节奏校准期限，确认同一等待未被重复扣除。
3. 恢复策略必须能完成连续的暖机与有效生成，避免交替拒绝反复暖机；在真实源断点时仍reset。对所有子帧都过期、只有前几个过期和短暂显示阻塞分别验证，不制造假帧或修改FPS计数。
4. 检查单owner同步Present及上一批GPU完成等待是否阻断可安全准备的下一帧；先量化，再在现有资源所有权和有界队列内修复，与5080工作连续性排查共同推进。
5. 增加受控时间线回归：2X/4X/6X，30/59.94/60源，周期性17/33/80ms呈现阻塞，一次拒绝、交替拒绝、连续恢复、样本老化、真实GPU超预算和输入断点。断言有效生成恢复、每类丢帧原因、队列上限及历史正确性，而不是只断言admit返回值。再同参数120秒实机对照，分别报告FG执行耗时、有效生成、显示提交、过期和恢复时间；30/40/50系验收独立记录。

## 3. 施工顺序

### P0：把每次等待和丢帧归属测清

1. 在现有诊断中增加会话内增量：实际来源类型、输入到达、解码完成、图提交/完成、呈现入队、提供方调用开始/返回。绑定 source ID、epoch、revision；统计固定窗口 P50/P95/P99/max，不逐帧刷生产日志。
2. 区分输入 mailbox 覆盖、主动限帧、性能跳过、生成失败、显示淘汰；reset 记录原因与前后源帧身份。
3. 同步状态拆成用户请求、应用等待、提供方调度、实际 sync/tearing flags。提供方生成帧记录全序列间隔，包含批次边界，并解码 SDK 返回码。
4. 利用上一轮已有回调锁等待/复制指标，补齐回调入口/出口、驱动 PTS、协商格式和 allocator；确认诊断不会显著拉低吞吐。

### P1：XeSS 呈现与历史连续性

5080 优先做同输入三组120秒对照：NR only、XeSS4X only、NR+XeSS4X；固定原生4K30、画布和显示模式，排除主动限帧与参数切换。分别测增强图 GPU、XeSS 提供方 GPU、XeLL sleep、媒体 deadline、proxy Present 和最终子帧调度；同步采功率/核心频率/显存/利用率/节流原因，并用共同单调时钟对齐。无5080时本机只验证机制，不能代替该用户功耗验收。

必须用 GPU 时间线或等价可验证的队列证据区分“Present 在等 GPU 执行”和“CPU 等显示导致下一批 GPU 工作未提交”。如果确认后者，优先在现有有界调度器内调整准备/提交顺序，让允许独立的下一源帧准备利用等待时间；保留 XeSS tag->Present 期间同队列资源状态和消费者 fence 合同。不能直接把 Present 搬到另一个线程并同时复用纹理。若涉及独立队列/线程，另列资源所有权和GPU验证矩阵后实施。只在测量证实期限重复时合并等待，不能同时撤掉所有节奏控制或用忙等耗电“稳定功率”。验收目标是减少稳态长帧和异常生成缺口，不是强求瓦数恒定。

1. 基于现有 VSync 改动验证提供方最终 Present；完全关闭仍必须生效。低排队/Reflex 等应用等待不叠到 XeSS 调度器上，界面说明实际生效范围。
2. 分解 Present 内等待、GPU ready 和图线程被阻塞的位置。检查资源生命周期、共享 GPU 所有权及锁持有范围；有证据后缩短阻塞，不用无界队列换表面高 FPS。
3. 沿用已有 XeSS 原生 scheduler 接入、零 render-time 反馈修复；核对用户包是否包含这些修改。2X 与多帧路径分别验收，避免重新引入双重等待。
4. 核对 enable/reset/Frame ID：断点当前真实帧正确入历史，下一对连续帧恢复；重复重绘不生成。保留必要历史重置，不通过拖动/转身时停止生成、重复原帧或改计数掩盖问题。

### P1：GC573 RGB60 回调与协商

1. 同卡同信号，分别运行 OBS 与 Veyra，记录实际 AM_MEDIA_TYPE、AvgTimePerFrame、尺寸、subtype、stride、方向、allocator 和音频路径。不能只对比界面都显示“60”。比较 60 与 60000/1001 时使用真实有理数。
2. 先全效果关做入口测试，再单 NR、XeSS 4X、组合；音频开关仅作定位变量。检查 graph clock/交付节奏，而非直接移除时钟。
3. 若复制/锁确为瓶颈：将驱动回调压缩为有界的应用自有 packed RGB 复制，复用既有 GPU 解包路径；保留 top-down、翻转、stride、range/matrix 合同。必要 CPU 回退优先复用已有成熟矢量化实现并记录来源。
4. 不长期持有驱动样本，不复用消费者仍持有的帧，不增加无界排队；保持 latest-frame mailbox。不能通过强制换 NV12、重复帧或隐藏实际 FPS 作为 RGB60 修复。

### 独立审查项

此前全软件审查的压缩包队列/帧身份/EAGAIN/PTS、D3D11 context4 释放、字幕锚点重叠/缓存键遗漏，另行回归处理。它们是代码审查发现，不是三份日志全部症状的共同解释。

## 4. 验收与交付

- 修复前后使用同输入、同格式、同效果、同显示刷新率和驱动设置；每个稳定场景 120 秒，单次测试不超过 300 秒。过渡阶段单列，不能删掉失败窗口制造平均值。
- GC573：原生 RGB 4K60 全效果关闭，比较回调入口、复制完成和处理吞吐；目标为跟随真实输入 60/59.94，无持续 53fps 平台。以同条件 OBS/驱动 PTS 为基准，缺实卡则明确未验收。检查 RGB 方向、颜色、花屏和音画同步。
- XeSS：2X/3X/4X，60fps 与 30fps 来源，关闭/VSync 两类显示条件，静止、持续转身、平移、resize、设置切换、断点恢复；DLSS 2X/4X/6X 作回归对照。当前 XeSS 上限 4X，不把 DLSS 6X 误写为 XeSS 支持。
- 统计全输出序列的长短间隔、每秒有效生成量、掉帧/reset/警告及队列上限；独立检查显示事件和可见撕裂。提供方 submitted/presented 计数不替代物理屏幕刷新证据。
- 功率须另采带时间戳的硬件遥测，判断是否跟随工作断续；不能从利用率单独归因。屏幕端到端延迟须外部测量，软件日志只报告进程内指标。
- 构建、相关单元/集成测试、GPU debug layer 和真实来源回归后再写验收结论；无实卡或显示设备的场景列为待用户验证。不得用本机 RTX5070 替代 5070 Ti/5080/5090/GC573 全型号验收。

初次方案阶段仅读取原日志与代码。后续用户授权施工，存档 `5a1931b`，隔离分支 `codex/fg-scheduling-repair-20260919`；实现及实际验收见 [补帧调度验收](FG_SCHEDULING_ACCEPTANCE_2026-09-19.md)。日志、测试、临时产物在 `E:/项目/Veyra/{tests,logs,tmp}/xess-gc573-20260919/`，构建复用 `E:/项目/Veyra/build/slider-reset-20260919/`。GC573 日志所用 packed RGB 已走 GPU 解包，不能继续将 CPU 逐像素扩展写成已确认根因；实卡 53fps 和 5080 功耗仍待验证。

# RTX3060 DLSS FG freeze: r4 candidate

Branch: `codex/post140-field-repair-20260917`. No merge, push or release.
Status: implementation and local regression complete; actual RTX3060 2X/6X
execution remains unverified. Recovery to playback is not FG support.

## Evidence and diagnosis

User files: `C:/Users/123/Desktop/logs/fg-probe-{10060,1872,2808}.log`
and `veyra-app.log`. RTX3060 12GB, device 0x2504, driver 32.0.16.1047.
DLSSG 310.7.0.0 SHA256
`135EAF0733C1E37381A8C28ABCF7A862404A54132B81787C04E35D09EFC5E36F`.
Create returns 0x1/non-null handle; warmup succeeds; formal Evaluate fails
0xBAD00002 / SEH 0xC0000005 at frameId2/subframe1. Probe stays at stage3
until cancellation (about 6/13/36 seconds in the three captures).
The log establishes failed evaluation and stalled teardown, not GPU saturation.
The exact teardown lock is not observable from these logs.

r3 spoofed Blackwell globally and rebuilt all 69 inventoried fatbins. Upstream
RTX40MFG-Unlock commit `33b41835dc39c5d8ab1ef93efb2449be31139c09`
preserves the real architecture and rebuilds 25 registered programs plus font.
Global spoof can alter architecture-specific network selection. This is a
candidate explanation for the target failure, not confirmed RTX3060 causality.

Local controlled comparison (RTX5070/616.56): forcing reported Ampere produces
25 NGXCubinD3D12 CreateKernel failures (nvapi -1), EndpointDL4RT creation
failure, then a null-pointer access at provider RVA 0x6BBF0. Preserving real
Blackwell with the same rebuilt program path succeeds. CUDA preflight and
NGX Create success alone do not prove usable frame generation.

## Changes

- `src/ngx/AmpereMfgUnlock.cpp`, corresponding header: rebuild only registered
  25 programs; publish pointer and supplied length; preserve compressed-entry
  expanded-header semantics from upstream. Add exact-hash font PTX adaptation,
  in-place publication and per-page restoration including Windows private
  WRITECOPY behavior. Keep network and other auxiliary blobs untouched.
- CUDA module/function preflight now checks all 26 programs on the D3D12
  adapter's LUID, instead of assuming CUDA ordinal0; restore the caller context.
- `src/pipeline/EnhanceGraph.cpp`: retain real NVAPI architecture by default;
  keep existing scoped capability/startup/Create compatibility and 6X ceiling.
  Spoof is diagnostic-only. Logs distinguish inventory from actual mutations.
- `src/engine/FgCompatibilityProbe.cpp`: failed actual initialization/evaluation
  terminates the disposable child before poisoned-provider teardown. Enable
  provider logging in that child. Parent retains existing explicit error and
  base-playback recovery; no fabricated generated frames.
- `tests/integration/FgSettingsTests.cpp`: test actual provider failure through
  the child and resumed playback within 15 seconds.
- Correct source comments and `THIRD_PARTY_NOTICES.md`. All provider writes
  remain process-local; on-disk runtime binaries are unchanged.

## Actual commands and results

Build: `cmd.exe /c out\build\3060-incremental.cmd`, exit0. Final build output
`out/logs/3060-r4-build.log`. Existing FFmpeg conversion warnings remain.
Product EXE SHA256:
`4DB42A6D9D74065FA5708FA9A7755FF6751323A44488F6D0BB710AF0724D5428`.

All tests used `scripts/run-short-test.ps1`; environment overrides were set
only in separate test shells. Paths below are relative to the repository.

| Test | Command arguments / override | Evidence and result |
| --- | --- | --- |
| Ampere lifecycle | `veyra_fg_lifecycle_tests.exe`, timeout45, `VEYRA_TEST_FORCE_AMPERE_UNLOCK=1` | `out/logs/3060-r4-real-arch-lifecycle-final.stdout.log`, PASS: 6,2,5,3,4,6; 26/26 CUDA preflight, Create/Evaluate, reset and release |
| Ada regression | Same executable, timeout45, `VEYRA_TEST_FORCE_ADA_UNLOCK=1` | `out/logs/3060-r4-ada-lifecycle.stdout.log`, PASS |
| Settings | `veyra_fg_settings_tests.exe out/logs/rtx30-package-motion-fixture.mkv out/logs/3060-r4-settings`, timeout100, force Ampere | `out/logs/3060-r4-settings.stdout.log`, PASS 1->2->6->1->6, pause/seek/resume, 310 source/167 generated |
| Actual failure recovery | Settings executable plus `probe-failure`, output `out/logs/3060-r4-probe-failure`, timeout30, force Ampere and `VEYRA_TEST_NVAPI_SPOOF_ARCH=0x170` | PASS: child30756 exit1/stage-1 after actual Evaluate AV; parent advances from12 to24 source frames in3107ms; `logs/fg-probe-30756.log` contains original internal errors |
| 6X content | `veyra_repair_fg_tests.exe mfg6-exact-rgb-planar-detail`, timeout90, force Ampere | `out/logs/3060-r4-content.stdout.log`, exit0; real interpolated content checked with existing position criterion |
| Delivery | `scripts/gates/delivery.ps1 -Root . -BuildDirectory out/build/scheduling-audit-20260918` | `logs/delivery/dc4993094fea4a4a8f6bdd15b3ab78e5/result.json`, PASS61.41s, native5070 NR/FG/4K/export checks |

Initial r4 font publication failed because the generic patch helper required
WRITECOPY to remain numerically WRITECOPY after privatization. Evidence retained
in `3060-r4-real-arch-lifecycle.stdout.log`; fixed by adapting upstream's private
working-set proof and rerunning the lifecycle successfully. Earlier forced
architecture failures in `3060-upstream-header` and `3060-native-arch` remain
research evidence, not target GPU tests.

## Remaining verification

Local package:
`C:/veyra-test-packages/post140-20260918-r4/Veyra-1.4.0-test-20260918-r4-win64-portable.zip`
(464935003 bytes), SHA256
`6F4DF97C899FF8EDB028AC6769A78932199C99233F53170615DE7014662B9460`.
Created with `scripts/package-portable.ps1 -Root . -Version 1.4.0 -Label
'-test-20260918-r4' -OutputDirectory C:/veyra-test-packages/post140-20260918-r4
-BuildDirectory out/build/scheduling-audit-20260918`, finalized/audited with
`out/tmp/finalize-post140-package.ps1 -Revision r4 -SkipSource`.
113 archive files verified against the package manifest; proprietary runtime
identity and source/media/config exclusions checked; patched FFmpeg retained.

Extracted-package smoke command: `out/tmp/smoke-post140-package.ps1 -Revision
r4 -CaseFilter forcedAmpere6 -OutputSuffix '-ampere' -MediaOverride
'C:/Users/123/Desktop/Veyra DLSS Video Player/out/logs/rtx30-package-motion-fixture.mkv'
-RequireNonblank`; repeated with `forcedAda6` and `'-ada'`.
Both pass on RTX5070 with actual NR and FG, package-local DLL resolution,
85/295 and 116/450 source/generated frames, nonblank screenshot pixel checks,
and exit0. Evidence: `out/logs/package-post140-smoke-20260918-r4-final-{ampere,ada}/result.json`.
Source snapshot is generated separately after this record is finalized.

No RTX3060 or RTX40 physical execution was performed locally. User reported
RTX40 unlock working on the earlier candidate. r4 needs RTX3060 2X followed
by 6X, then NR/SR combinations; retain application and all fg-probe logs.
Existing content-dependent synthetic 6X position deviations remain documented
in the main repair plan. Export failure teardown is outside this isolated
preview probe fix; the user's export-gate removals remain in this build.

Do not mark the broader 30-series goal achieved until actual generated output
and stability have been verified on the target card. Next task: evaluate r4's
RTX3060 application/probe logs and visible output from that test.

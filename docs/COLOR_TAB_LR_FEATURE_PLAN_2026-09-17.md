# 色彩选项卡（Adobe LR 功能对齐 + .cube LUT）实施方案 · v4

日期：2026-09-17（v4：调色链**全部前移到所有效果器之前**，单一融合 pass，可完全关闭）
状态：**方案定稿；P1 唯一授权范围；T0/T1/T2a 已交付，T2b 起按本版顺序施工**

已锁定决定（2026-09-17 用户批复）：

1. 原"预设"页代码级删除、不留入口；`last-applied.v1` 与 `user-presets.v1` **保留但无 UI**；
2. 色彩预设支持**保存/命名 + 导入/导出文件**（P1 内交付）；
3. **镜头模糊不做**（不具备深度来源，直接砍掉，不进任何阶段）；
4. **本次只做 P1**，P2–P4 未授权；"仅显示已改动"取消，改为顶部一键还原。
5. **调色链全部前移**（2026-09-17 追加）：白平衡/曝光/曲线/HSL/黑白/分级/校准/LUT 一律落在
   **所有效果器之前**，直接作用在源头线性图上；**不新增多余链路**——全部融合成同一个 pass，
   后续的清晰度/去朦胧/锐化/颗粒/晕影若要做，也必须并进这同一个 pass，不允许再开新 pass。
6. **调色链可完全关闭**：色彩页给总开关。关闭时链路**不存在**（不创建资源、不 dispatch、
   不改下游采样点）= 零开销，给要超低延迟的用户用。
范围：

1. 删除"参数与预设"窗口里的 **预设** 页（**代码级删除，不留任何入口**），替换为 **色彩** 选项卡；
2. 色彩按 Adobe Lightroom（对照 15.5）的调节面板组织，**在浮点线性域里调，不做 8 位滤镜**；
3. 支持导入 `.cube`（1D/3D），并允许用户**保存/命名色彩预设**，便于按游戏切换；
4. HDR 内容按 HDR 标准（BT.2100 / BT.2408）处理，不是把 SDR 参数硬套；
5. 顺带修增强面板的 **NR 保护区域 → NR 剔除区 + 羽化滑块**（现状不可用）。

不在范围：AI 蒙版（主体/天空/人物/风景）、画笔/深度蒙版、生成式移除与内容识别修复、内容凭证、ICC 软打样/色域警告、相机 DCP 配置文件库、Process Version。

---

## 0. 结论摘要

- 能做到**功能对齐 + 参数语义对齐 + 专业级观感**；做不到**逐像素等于 Lightroom**（相机配置文件、色调映射、AI 模型都是闭源黑盒）。任何"完全一样"的说法都是撒谎。
- 调色跑在**现有 FP16 线性工作图**上（`EnhanceGraph` 的工作纹理本来就是 `R16G16B16A16_FLOAT`），所以"高精度"这一条**不花额外延迟**；花延迟的是 pass 数量与全分辨率读写次数。防色带按"浮点链路 + 高精度 LUT + 输出抖动"三条落实。
- HDR 按标准做：线性光里调，PQ/HLG 走 BT.2100 EOTF 转换，SDR 参考白沿用代码里已有的 **203 cd/m²**（BT.2408）合同，导出重新计算/标注 MaxCLL / MaxFALL。
- 命名预设是**色彩外观预设**（含 LUT 引用），不恢复旧的增强参数预设页。

---

## 1. 研究结果：Adobe Lightroom 最新版本基准（2026-09-17 实读官方页）

来源（浏览器实读，非二手转述）：

- `helpx.adobe.com/lightroom-classic/desktop/introduction-to-lightroom-classic/whats-new.html`（页面标注 Last updated **Aug 4, 2026**）
- `.../process-and-develop-photos/image-tone-color.html`、`color-mixer.html`、`lens-blur.html`、`enhance-details.html`、`masking.html`
- 时间线交叉核对：The Lightroom Queen《15.5》(2026-08-03)、《15.0》(2025-10-28)

**当前版本：Lightroom Classic 15.5（2026 年 8 月）**，Lightroom 桌面 9.5 / 移动 11.5。

| 版本 | 与调色/编辑相关的变更 |
| --- | --- |
| 15.5（2026-08） | 蒙版新增 **Feather / Edge** 精修滑块；裁剪不透明度；Render to DNG |
| 15.4（2026-06） | 主体选择精度提升；重复图检测；Faces 面板 |
| 15.3（2026-04） | **Film-inspired 预设组**；AI 编辑后台执行；PSB |
| 15.2（2026-02） | Edit in Firefly；WebP |
| 15.0（2025-10） | **Point Color 新增 Variance**；Assisted Culling；裁剪缩放；目录升级 |
| 14.4 / 14.2 | Enhance 不再强制新建 DNG；Distraction Removal；**Adaptive Profiles** |

要点：**15.5 的调色滑块集合与 14.x 一致**，这一年新增集中在蒙版精修、AI 辅助与预设，所以照 LR 组织面板不存在"版本追不上"的问题。

---

## 2. LR 调节功能全清单 → 我们的取舍（v2 更新）

**阶段**：P1 = 第一版交付，P2/P3/P4 = 后续；"不做" = 明确不实现并写原因。

### 2.1 配置文件 / 白平衡

| LR 功能 | 我们的做法 | 阶段 |
| --- | --- | --- |
| 相机配置文件、Adaptive Profiles、配置文件量 | 不做 DCP/相机配置文件库；改提供自研"渲染基底"下拉（无 / 中性 / 胶片感），承担"先定基调"职责 | P3（可选） |
| 色温/色调、白平衡预设、中性灰吸管、自动 | 全做。白平衡在**场景线性**域走 CAT（Bradford/CAT16）适应矩阵，而不是 RGB 增益；刻度明确标注为**相对值**（视频/采集没有相机 as-shot 语义） | P1 |

### 2.2 亮 / 颜色

| LR 控件 | 我们的做法 | 阶段 |
| --- | --- | --- |
| 自动、曝光、对比度、高光、阴影、白色、黑色 | 7 项全做（线性域曝光 + 平滑分区色调曲线）；"自动"基于直方图分析 | P1 |
| 直方图 + Alt 裁剪预览 | 做（GPU 降采样 + 低频统计读回，含逐通道裁剪提示） | P1 |
| 自然饱和度、饱和度 | 单 pass，在保持明度的感知空间做（参考 darktable `colorbalancergb`，含肤色保护） | P1 |
| 纹理、清晰度、去朦胧 | **并入源头同一个 pass**（局部对比/去雾用邻域采样在同一 dispatch 内完成），不再新增 pass；去朦胧对夜景/高光内容的副作用单独验收 | P2 |

### 2.3 曲线 / 混色器 / 点颜色

| LR 控件 | 我们的做法 | 阶段 |
| --- | --- | --- |
| 参数曲线（4 区 + 3 个范围分割）、点曲线（RGB + 单通道）、预设 | 全做。曲线烘焙成 **FP16、1024 项 1D LUT**（每通道），GPU 一次采样，不做 8 位量化 | P1 |
| 目标调整工具（TAT） | 做 | P2 |
| HSL 三页：色相/饱和度/明亮度 × 8 色 | 全做，色相区间用重叠平滑权重，避免硬边与色带 | P1 |
| "颜色"页（按颜色的另一种分组） | 同一份参数的另一套 UI 分组，零额外算法成本 | P1 |
| 黑白混色器（8 色 + 自动 + TAT） | 全做，与 HSL 共用色相权重 | P1 |
| 点颜色：吸管、色相/饱和度/明度位移、**变化(Variance)**、范围、可视化范围 | 做 6–8 个采样点，色度距离加权；"可视化范围"做成叠加调试层 | P2 |

### 2.4 颜色分级 / 校准 / 效果 / 细节

| LR 控件 | 我们的做法 | 阶段 |
| --- | --- | --- |
| 颜色分级：阴影/中间调/高光/全局四色轮 + 混合 + 平衡 | 全做（自绘色轮 + H/S/L 数值框） | P1 |
| 校准：阴影（绿-品红）、红/绿/蓝原色（色相/饱和度） | 全做（3×3 原色矩阵 + 阴影 tint） | P1 |
| 效果：颗粒（数量/大小/粗糙度）、晕影（数量/中点/圆度/羽化/高光）、裁剪后晕影 | 做（参考 darktable `grain` / `vignette`），按 v4 **并入源头 pass**；颗粒从此会经过 NR/SR/补帧再加工，验收必须对比实际观感，不达标就回退为不做 | P2 |
| 细节：锐化（数量/半径/细节/蒙版） | 做（USM + halo 抑制 + 边缘梯度蒙版），同样并入源头 pass；锐度会与 NR/SR 叠加，需做叠加验收 | P2 |
| 细节：噪点消除（明亮度/颜色等 6 项） | 部分做：明亮度/颜色两档实时降噪；重度降噪沿用现有 **DLSS-NR**，UI 标注"AI 降噪由现有增强承担"，不逆向 LR 模型 | P2 |

### 2.5 光学 / 镜头模糊 / 蒙版 / 修复

| LR 功能 | 我们的做法 | 阶段 |
| --- | --- | --- |
| 手动：扭曲度、晕影、去边（紫/绿边 + 数量） | 做（色相键控 + 边缘扩散） | P3 |
| 镜头配置文件（按 EXIF 自动校正） | 仅文件播放路径做：EXIF 镜头 → Lensfun（LGPL-3）；**采集卡没有 EXIF，不给假选项** | P3（可选） |
| 镜头模糊（散景形状、模糊量、猫眼、增强、焦距范围、主体/点对焦、可视化深度、画笔精修） | **不做**（2026-09-17 用户决定）：前置条件是深度推理，而产品当前没有任何深度来源（无 DirectML/ONNX/TensorRT），不值得为它引入一个模型 | — |
| 线性渐变 / 径向渐变蒙版 | 记录在案，**未授权**（需要时单独评估） | 未定 |
| 画笔 / 颜色范围 / 明亮度范围 / 深度范围蒙版 | 暂不做 | — |
| AI 蒙版（主体/天空/背景/风景/对象/人物） | **不做**：需要分割模型 + 时域一致性，成本远超调色本身 | — |
| 修复/仿制、内容识别移除、生成式移除、除尘/去反光 | **不做**：视频逐帧本地生成式修复不现实；云端方案与本地播放器定位冲突 | — |

### 2.6 增强 / HDR / 预设（本次明确要求的修改）

| 项目 | 结论 | 阶段 |
| --- | --- | --- |
| Denoise / Raw Details / Super Resolution | 已有对应能力（DLSS-NR / 锐化降噪 / DLSS·RTX Video SR·FSR），色彩页只做入口与说明 | — |
| HDR 编辑与输出 | 做，且**按 HDR 标准做**（见 §5），不使用 SDR 语义硬套 | P1 |
| 软打样 / 色域警告 / ICC 打印模拟 | 不做 P1–P3（需要 ICC 引擎与参考显示器语义） | — |
| **色彩预设（用户命名，按游戏切换）** | **做**：保存 / 另存为 / 重命名 / 覆盖 / 删除 / 快速下拉切换 / **导出为文件 / 从文件导入**；内容 = 全套色彩参数 + 引用的 LUT（路径 + 强度 + 输入空间） | P1（核心） |
| 原"预设"页（NR/SR 参数预设） | **代码级删除，不留入口**（含主窗口日常模式的预设下拉，见 §8） | P1 |
| 历史 / 快照 | 撤销/重做 + "按住看原图" + 单槽 A/B 快照；完整历史面板 P3 | P1/P3 |
| 复制/粘贴色彩设置 | 做 | P1 |

### 2.7 .cube LUT

规格要点（Adobe Cube LUT Specification 1.0）：`TITLE`、`LUT_1D_SIZE` / `LUT_3D_SIZE`、`DOMAIN_MIN`/`DOMAIN_MAX`（默认 0/1）、`#` 注释；数据为 0–1 浮点三元组；**3D 数据中第一分量（红）变化最快**；常见尺寸 17/33/65。

实现要求：

1. 支持 1D 与 3D；尺寸、域、行数、NaN 全部校验，不合格**拒绝导入并保留原文件**。
2. GPU 侧 = `Texture3D`（**RGBA16F，不用 8 位**）+ **四面体插值**（精度优先；65³ 可退化为硬件三线性以省带宽）。
3. 单个 65³ LUT ≈ 4.4 MB 显存；同时加载上限 8 个 + LRU 缓存；落地 `runtime_local/luts/*.cube` + manifest（名称、来源、尺寸、SHA-256）。
4. 控件：强度、输入空间（Cineon Log / sRGB 显示参考 / PQ）、启用开关；固定顺序"颜色分级之后、颗粒之前"。
5. 只接受 `.cube`；`.3dl/.mga/.look/.csp/HaldCLUT` 明确不支持。

---

## 3. UI 设计：色彩选项卡 + 风琴折叠（v2 更新）

### 3.1 结构

- "参数与预设"窗口页签：**增强 / 运动 / 色彩 / 导出**（"预设"页整页删除）。
- 色彩页 = 顶部工具条 + 可滚动风琴分组列表。
- **顶部工具条**：色彩预设下拉（+ 保存/另存为/重命名/删除/**导出到文件/从文件导入**）、导入 LUT、**一键还原（回中性，保留一次撤销机会）**、按住看原图、复制/粘贴色彩设置、撤销/重做。
  - 按批复：**取消"仅显示已改动"开关**；取而代之的是顶部"一键还原"与每组右键"重置该组"。

### 3.2 分组顺序（与你截图一致）

1. 亮　2. 颜色　3. 曲线　4. 混色器（HSL/颜色/黑白）　5. 颜色分级　6. 细节　7. 效果　8. 光学　9. 校准　10. 点颜色　11. LUT

### 3.3 控件规格（对齐 LR 的操作习惯）

- **分组头**：左折叠三角 + 名称；右侧**眼睛** = 该组 bypass（灰=未启用）。单击折叠/展开；右键 = 重置该组 / 复制该组。
- **滑块行**：标签 + 数值框（可输入）+ 滑轨；拖动实时生效；**双击复位**；Alt+拖动精修；方向键 ±1 步；`Home` 回中性。
- **色轮**：自绘（GDI+），中心拖动 = 色相/饱和度，旁列 H/S/L 数值框。
- **曲线**：自绘网格 + 控制点增删拖拽 + 通道下拉 + "拉平曲线"。
- **LUT**：列表（名称/尺寸/强度）+ 导入 + 输入空间下拉。
- **状态记忆**：折叠状态、滚动位置、上次使用的色彩预设存进 `ui-preferences.v1`。
- **可访问性**：全部走 `dip()`；Tab 顺序按视觉顺序；每个控件配一句人话帮助文本（沿用 `installDialogHelp` 风格）。

### 3.4 色彩预设（新增，按游戏切换）

- 列表 + "保存当前设置为预设"（命名 ≤48 字，重名需确认覆盖）+ 另存为 + 重命名 + 删除。
- 预设内容：全部色彩参数 + LUT 引用（路径 + 强度 + 输入空间）；**不包含** NR/SR/补帧等增强参数（那属于设备/画质，不属于外观）。
- 存储：`color-looks.v1`（文本格式，坏文件保留不覆盖）；**导出/导入 `.vpcolor`（P1 交付）**，用于分享与备份。
- 切换预设时**不重建图、不重连采集**，只更新 uniform 与 LUT 资源。

---

## 4. 精度与色带：为什么不是"滤镜"，会不会掉延迟（新增章节）

### 4.1 坏做法 vs 我们的做法

坏做法（必然出色带）：源 → 8 位 sRGB → 8 位 LUT → 8 位输出，每一步量化都在砍信息。

我们的做法：

1. **全链路浮点**：`EnhanceGraph` 工作纹理本来就是 `R16G16B16A16_FLOAT`（线性光），调色 pass 直接在这层做，**不新增 8 位中间纹理**。
2. **LUT 高精度**：曲线烘焙 FP16/1024 项 1D LUT；3D LUT 用 RGBA16F + 四面体插值；不用 8 位 LUT。
3. **运算顺序按色彩学来**：曝光/白平衡在线性光里乘除；对比度/曲线/分级在有明确挂载域的显示参考域或对数域做，绝不在 PQ/HLG 码值上直接做算术。
4. **输出抖动（dither）**：进 8 位 SDR 呈现/导出前加三角抖动，把量化误差变成不相关噪声而不是色带。
5. **源头色带可选缓解**：P2 增加可选"去色带（deband）"滤波（梯度检测 + 低幅噪声，参考 libplacebo/mpv 的 deband），UI 与日志明确它只减轻、不修复源头量化。

### 4.2 会不会大幅增加延迟？——不会；代价来自 pass 数量，不是位深

- 工作图本来就是 FP16 → "在线性浮点里调"**不增加带宽**；改成 FP32 只会白花一倍带宽。
- 真正开销是"每多一个全分辨率 pass 就多一次读写"。v4 决定：**只有一条链、只有一个 pass**，
  位置在所有效果器之前，因此它作用的是**源头分辨率**（1080p 源 ≈ 4K 工作图的 1/4 像素）。

| 新增工作 | 目标耗时（RTX 5070） |
| --- | --- |
| 色彩 pass（白平衡/曝光/曲线 1D LUT/HSL/黑白/分级/校准/3D LUT，单一 dispatch） | 1080p 源 ≤ 0.15 ms；4K 源 ≤ 0.35 ms |
| 直方图统计（降采样 + 每 200 ms 读回） | ≈ 0 |
| **关闭总开关时** | **0 ms（pass 不存在）** |

相对 4K60 的 16.6 ms，1080p 源的固定成本约 0.9%，4K 源约 2.1%。新增 `GpuStage::Grade` 计时，进
`player-timing` / `capture-timing` 日志（与现有 `gpuSrP95Ms` / `gpuNrP95Ms` 并列）；超预算按
3D LUT 四面体 → 硬件三线性 → 关 LUT 的顺序降级并写日志，**不允许静默降级**。

---

## 5. HDR 按 HDR 标准做（v2 重写）

现状（代码事实）：项目已有 HDR 合同 —— `MediaFileSource` / `ColorMetadata.h` 解析 mastering、MaxCLL、MaxFALL；`HdrToneMap.h` 已用 **203 cd/m²（BT.2408 的 SDR 参考白）** 与 HLG 1000-nit 参考显示；预览区分"原生 HDR 旁路显示"与"HDR→SDR 增强"。色彩模块必须接着这条合同走。

### 5.1 工作空间与转换

- 工作空间：**场景线性 + BT.2020 原色 + FP16**，允许超过 1.0（HDR 高光不截断）。
- HDR 输入：PQ 用 BT.2100 EOTF（0.0001–10000 cd/m²），HLG 用 BT.2100 OOTF（1000-nit 参考显示、系统 gamma）；**先解析出绝对亮度语义再做线性运算**。
- 输出：按 sink 重新编码回 PQ / HLG，或走既有 HDR→SDR 路径；每步在日志写明传输函数与峰值来源（沿用 `hdr-metadata` 日志风格）。

### 5.2 各控件在 HDR 下的定义

| 控件组 | HDR 下的做法 |
| --- | --- |
| 曝光 | 线性光乘 2^EV，参考白/峰值随元数据变化，不改变相对内容 |
| 白平衡 / 校准 | 线性光 CAT / 3×3 原色矩阵，作用域与 SDR 一致 |
| 对比度 / 高光 / 阴影 / 白色 / 黑色 | 在**对数域或线性域**做分区曲线，参考点 = SDR 参考白 203 cd/m² 与内容峰值；不在 PQ 码值上直接加 |
| 曲线 | 明确挂载域：默认"对数域（Cineon/ACEScct 风格）"，可选 PQ 域；UI 标注当前域 |
| 混色器 / 点颜色 / 颜色分级 | 在保持亮度的感知空间做（OkLab 或 ICtCp，实现时定一个并写死），HDR 色域映射按 BT.2408 图形白与 BT.2020 容器处理 |
| LUT | 必须选输入空间：**Cineon Log（默认，创作者 LUT）** / sRGB 显示参考（SDR 内容或 SDR 参考域模式） / PQ；错误组合被拒绝并提示，**不允许静默按 sRGB 套 HDR** |
| 颗粒 / 晕影 | 线性域生成后按亮度归一化叠加，避免暗部被颗粒抬起色度 |

### 5.3 元数据与导出

- 调色改变峰值与内容亮度分布 → 导出时**重新计算并写入 MaxCLL / MaxFALL**（能测就测）；mastering 峰值无法测量时保留原值并标注"未更新"，不允许静默写入与实际不符的静态元数据。
- 字幕/OSD 属于图形白（BT.2408 图形白），不参与调色、不参与颗粒/晕影。
- 保持既有区分：HDR 原生旁路显示 ≠ HDR→SDR 增强；任何情况下不得把 SDR 结果冒充 HDR。

---

## 6. 技术插入点 / 持久化

### 6.1 插入位置（三入口共享一张图）

**v4 顺序（全部前移，只有一条链）**：

```
源（采集卡 / 文件 / 图片）
  → 上传 + YUV/打包格式 → 线性 FP16（源头分辨率）
  → 【色彩 pass】白平衡 · 曝光 · 曲线 · HSL/黑白 · 颜色分级 · 校准 · 3D LUT   ← 唯一位于效果器之前的调色
  → SR（DLSS / RTX Video SR / FSR，可选）
  → NR（DLSS-NR，可选） → 残差合成
  → 补帧（DLSSG / XeSS / FSR，可选）
  → 缩放 blit → 字幕/OSD 合成（不参与调色）→ sink（预览 / 截图 / 导出）
```

理由（按你的判断执行）：调色越早接触原始画面，作用的就是真实源像素，而不是 NR 重建/SR 放大之后的
像素；同时源头分辨率下做调色更便宜。代价写在 §10：效果器从此看到的是**带外观的画面**。

**总开关语义**

- 新增 `ColorSettings::enabled`（T2b 落地，默认 **关**，与"首次启动效果全关"的产品策略一致）；改任意色彩参数时自动打开（与 NR/超分开关的交互一致），UI 必须显示状态。
- 关闭时：**不创建**色彩 pass 的资源、**不 dispatch**、下游 SR/NR 的输入采样点直接指回未调色的线性图 → 与本轮之前的链路逐像素一致（验收项之一），开销为 0。
- 打开/关闭属于**链路形状变化**：允许重建图（和 NR/SR 开关同级别），但**改色彩参数本身不重建**，
  只更新 uniform / LUT 资源（`ColorSettings::enabled` 是 `sameVideoConfiguration()` 的比较项，
  其余色彩字段是排除项）。
- 三个入口（预览 / 截图 / 导出）共用同一张图与同一个 pass；导出时 `enabled` 与参数跟随当前设置。

### 6.2 设置模型与持久化

- 新增 `struct ColorSettings`（白平衡/亮/颜色/曲线/混色器/黑白/点颜色/分级/细节/效果/光学/校准/LUT），并入 `EnhancementSettings`。
- 色彩字段加入 `sameVideoConfiguration()` 的**排除清单**：改调色只更新 uniform / LUT 资源，**不重建图、不重连采集**。
- 落盘：`color-settings.v1`（当前）+ `color-looks.v1`（命名预设）+ `runtime_local/luts/` + manifest。
- `PresetStore` schema v17 → v18（行尾追加、旧文件可读）；预设页删除后，`user-presets.v1` 仅供迁移读取，不再有 UI。

### 6.3 开源搬运（落实 AGENTS"有开源就直接搬"）

| 来源 | 许可证 | 用途 |
| --- | --- | --- |
| darktable | GPL-3.0 | `lut3d`（.cube 解析 + 四面体插值）、`colorbalancergb`、`filmicrgb`/`sigmoid`、`grain`、`vignette`、`cacorrect`、`shadhi` |
| libplacebo | LGPL-2.1+ | `pl_lut_parse_cube` 解析对照；deband 参考 |
| OpenColorIO | BSD-3-Clause | CUBE 解析与 DOMAIN 处理参考 |
| Lensfun | LGPL-3.0 | 镜头配置文件（仅文件播放路径） |

每项写入 `THIRD_PARTY_NOTICES.md`（仓库、固定 commit、许可证、改动文件、说明），禁止只改名不标注。

---

## 7. 随本次一起做的修复：NR 剔除区 + 羽化（已核对源码）

现状事实：

- `ProtectionSettings::featherPixels` **已存在**（默认 2，`validate()` 允许 0–32），预设也已序列化；
- 抛物线已在用：`shaders/NrResidualComposite.hlsl` 里 `protection = smoothstep(0, featherPixels, edge)`，单位是**工作分辨率像素**；
- 但设置面板**没有暴露这个值**（只有复选框 206 "NR保护区域"、按钮 213 "框选区域"、214 "清除区域"），用户只能吃到 2 px 过渡——在 4K 工作图上等价于硬边。所以"边缘太硬、没法用"是事实。

修改内容：

1. 改名：`NR保护区域` → **`NR 剔除区`**（复选框 206、计数文本 `NR剔除区 · n/4`、按钮改"框选剔除区 / 清除剔除区"、帮助 206/213/214 与 `SettingHelp.h` 第 19 条）。
2. 新增**羽化滑块**：0–64 px（工作分辨率像素），旁边实时显示"≈ 画面高度 x.x%"，默认值 2 → **12 px**；数值框可输入、双击复位、方向键微调。
3. 帮助文案写清语义：剔除区 = **NR 改动在该区域被剔除/抑制、保留原图**；只管 NR，不保护超分与补帧（与现有实现一致）。
4. 同步放宽 `validate()` 到 64（同字段同单位，预设 schema 无需迁移）。
5. 验收：1080 与 4K 两种工作分辨率下羽化 0 / 12 / 64 三档对比截图无硬边；`TiledImageProcessor` 的 tile 重叠必须 ≥ 最大羽化，避免块边界穿帮。

这一项是**独立小改动**，可与色彩页分开验收、单独进包。

---

## 8. 预设页的删除清单（代码级删除，不留入口）

1. `apps/veyra/SettingsWindow.cpp`：预设页整页（静态 1105、下拉 300、命名 301、按钮 310–315、静态 1106）、`refreshPresets()`、WM_COMMAND 里 300/301/310–315 分支、设置窗口内对 `store` 的使用；
2. `apps/veyra/SettingsWindow.h`：`presetNames()` / `presetAt()` 导出接口；
3. `apps/veyra/ui/AppShell.cpp`：**主窗口日常模式的预设下拉 `DailyPreset`**（同样是预设入口：控件 + `refreshDailyPresets()` + WM_COMMAND 分支 + 帮助文本 + 布局槽位）一并删除；
4. `apps/veyra/ui/SettingHelp.h`：id 301/310–314 的帮助条目；
5. `apps/veyra/ui/AppShell.cpp` 页签数组 `L"预设"` → `L"色彩"`；
6. 相关 smoke/合同测试中的预设驱动项（预设 schema 往返测试保留，但不再从 UI 驱动）。

保留但**无 UI 入口**（2026-09-17 已确认）：`last-applied.v1`（启动自动套用上次增强参数）、`user-presets.v1`（只读迁移，防止老用户文件被误删）。这两条保留的是**行为**，不新增、不残留任何界面控件或快捷键入口。

---

## 9. 实施与验收（v3：本次只执行 P1）

### P1（第一版交付）

色彩页框架（风琴/滚动/状态记忆）、命名色彩预设、白平衡、亮、颜色（自然饱和度/饱和度）、曲线、混色器（HSL + 黑白 + 颜色页）、颜色分级、校准、LUT 导入（1D/3D + 强度 + 输入空间）、一键还原 + 按住看原图 + 复制/粘贴 + 撤销重做、全浮点链路 + 输出抖动、HDR 按 §5、预设页与日常预设下拉的代码级删除、NR 剔除区 + 羽化。

**P1 里程碑拆解（按可独立验收的顺序）**

| 里程碑 | 内容 | 独立验收点 |
| --- | --- | --- |
| T0 | 删预设页 + 删主窗口日常预设下拉 + 帮助/布局清理（§8 清单） | 代码里不再有预设入口；旧 `user-presets.v1` / `last-applied.v1` 仍能读，行为不变 |
| T1 | NR 剔除区改名 + 羽化滑块（§7） | 1080/4K × 羽化 0/12/64 对比无硬边；tile 重叠 ≥ 最大羽化 |
| T2 | `ColorSettings` 数据模型 + 持久化（**已完成，T2a**）；源头侧单一融合 pass + 总开关 + `gpuGradeP95Ms`（T2b） | 改参数不重建图；`gpuGradeP95Ms` ≤ 0.15 ms @1080p 源 / ≤ 0.35 ms @4K 源；色带测试通过；关开关时与旧链路逐像素一致 |
| T3 | 色彩页 UI 框架（风琴折叠、滑块+数值框、双击复位、分组眼睛、一键还原、状态记忆） | UI 合同测试（命中/拖拽/键盘/DPI/折叠记忆） |
| T4 | 面板填充：亮、颜色、曲线、混色器（HSL/颜色/黑白）、颜色分级、校准 | 每个面板的 shader 单测 + 真机对比截图 |
| T5 | LUT 导入（校验/缓存/manifest/输入空间）+ 色彩预设（保存/命名/切换/删除 + `.vpcolor` 导入导出） | 坏 LUT 被拒且保留原文件；预设重启后记忆；切换不重建图 |
| T6 | HDR 标准处理 + 元数据 + 三入口一致性 + 全量回归与交付报告 | HDR PQ/HLG 各一轮；预览/截图/导出像素一致；旧预设文件可读 |

验收：

1. 新增 GPU 耗时：1080p 源 ≤ 0.15 ms、4K 源 ≤ 0.35 ms（含直方图），给出 `gpuGradeP95Ms` 与基线对比；
1b. **总开关关闭时**：不产生色彩 pass 的 dispatch/资源，预览输出与"本轮之前"的构建逐像素一致（对照截图 + 帧哈希）；
1c. **效果器输入变化验证**：同一素材、同一 NR/SR/补帧设置，比较"无外观 / 弱外观 / 强外观（大曲线 + LUT）"三档下
   NR 是否稳定、SR 是否有新伪影、补帧是否仍连续；出现明显退化就必须在文档和 UI 里写清限制，不允许默认开到最强；
2. 渐变天空测试图（8 位源 + 高强度曲线 + LUT）**无新增色带**：与未调色对比，用 10 位导出做量化检查（脚本 + 目视）；
3. 同参数下预览 / 截图 / 导出像素在容差内一致；
4. 改色彩参数不触发图重建、不重连采集（日志 `revision` 不变、无 reset）；
5. HDR：PQ 与 HLG 素材各一轮，线性域调色后导出的 MaxCLL / MaxFALL 与实际一致；LUT 输入空间选错被拒绝并有提示；
6. 色彩预设：保存/命名/切换/删除/重启后记忆；切换不重建图；
7. NR 剔除区：羽化 0/12/64 对比 + 1080/4K 无硬边；tile 重叠 ≥ 最大羽化；
8. 旧预设文件（v17）可读不丢；设置窗口内不存在任何预设入口（含日常模式下拉）。

### P2 / P3 / P4

- P2：纹理/清晰度/去朦胧、锐化 + 轻量降噪、颗粒 + 晕影、点颜色、TAT、直方图裁剪提示、可选去色带滤波。
- P3：光学手动三件套、Lensfun 镜头配置文件（仅文件路径）、完整历史面板与命名快照、预设文件导入/导出。
- 已砍：镜头模糊（无深度来源，用户 2026-09-17 决定不做）。
- 未授权：线性/径向渐变蒙版等超出 P1 的项目，需要时另开方案。

---

## 10. 风险与明确不做

**风险**：

1. **效果器看到的画面被改了**（v4 最大的代价）：DLSS-NR / RTX Video SR / FSR / DLSSG 都是在"自然画面"上训练的，
   把强曲线/强 LUT 塞到它们前面，可能出现 NR 判断变化、SR 伪影、补帧不连续。已在 §9 加了 1c 验收项，
   并且总开关默认关闭——最坏情况用户关掉调色就回到今天的画面。
2. LUT 在 HDR 上的错误映射（输入空间选择 + 拒绝错误组合 + 日志兜底）。
3. 曲线/色轮是新自绘控件，需要 UI 合同测试。
4. 观感与 LR 存在差异（文档与 UI 都写明"语义对齐，非逐像素复刻"）。

**明确不做**：AI 蒙版、画笔/深度蒙版、生成式移除与内容识别修复、内容凭证、ICC 软打样/色域警告、相机 DCP 配置文件库、Process Version，以及本次已取消的"仅显示已改动"。

---

## 11. 决策状态与开工前置

**已确认（2026-09-17）**

1. `last-applied.v1` / `user-presets.v1` 保留但无 UI（不残留任何控件或快捷键入口）；
2. 色彩预设支持保存/命名 **+ 导入/导出文件**（P1 内交付）；
3. 镜头模糊**不做**；
4. 本次**只做 P1**（T0–T6），P2–P4 未授权；去色带滤波留在 P2 清单，未授权。
5. （v4 追加）调色链**全部前移到所有效果器之前**、**单一融合 pass、不新增链路**；
   色彩页提供**总开关**，关闭时链路不存在、零开销（2026-09-17 用户指令）。

**开工前置（按项目规则，动手前先做）**

1. 施工在隔离分支进行，开工前先建 Git 存档点（checkpoint 分支），P1 全程不合并 main；
2. 先在隔离分支落地 T0 / T1（两项独立小改动，可各自先验收）；
3. 从 T2 起每完成一个里程碑更新 `docs/WORKLOG.md`（命令、结果、日志路径、失败与修复），未验证项如实写"未执行"；
4. 全部完成后交人工验收：验收合格才允许合并 main；发布另行授权。

**当前状态**：本方案为定稿，**代码零改动**，等待开工指令。
